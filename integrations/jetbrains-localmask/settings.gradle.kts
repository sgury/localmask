plugins {
    // Auto-downloads the JDK the build needs (17) — no manual install.
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.8.0"
}

rootProject.name = "localmask-jetbrains"
