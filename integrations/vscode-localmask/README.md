# LocalMask — Key Toggle

One key (`Cmd+Alt+K` or the status-bar key) flips the active editor between
real values and the masked view (`~[TOKEN]~`) — exactly what the AI sees.
Masking runs 100% locally via the LocalMask CLI.
